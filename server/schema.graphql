type Post {
  id: ID!
  authorName: String!
  message: String!
}

type Query {
  ok: Boolean!
  posts: [Post!]!
}

type Mutation {
  createPost(authorName: String!, message: String!): Post
}

type Subscription {
  postCreated: Post
}
